import { generate } from 'random-words';

const generateRandomDomain = (): string => {
    return 
}

const generateTunnelDetails = (port: number, subdomain: string) => {
    const randomDomain = 
};
